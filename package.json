{
  "name": "threatlens",
  "version": "1.0.0",
  "description": "ThreatLens — URL Intelligence Platform with AI analysis",
  "private": true,

  "engines": {
    "node": "18.x"
  },

  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },

  "dependencies": {
  },

  "devDependencies": {
    "vercel": "^33.0.0"
  },

  "notes": {
    "howToRun": "Run 'npm install' then 'npm run dev' to test locally. Run 'npm run deploy' to go live.",
    "apiKey": "Set COHERE_API_KEY in Vercel dashboard under Settings → Environment Variables"
  }
}
